# Issue 19: README usage docs

**优先级：P0** | **预估：S (human: ~1.5h / CC: ~15min)**

## 描述

编写完整的 README.md 使用文档，覆盖安装、CLI、配置、规则说明和报告格式。

## 产出

- `README.md`（覆盖现有占位内容）包含：
  - **项目简介**：一句话说明工具用途
  - **安装**：`npm install -g miniapp-audit` 和 `npx miniapp-audit` 两种方式
  - **快速开始**：一行命令扫描项目
  - **CLI 命令参考**：
    - 基本用法
    - `--format` 参数（markdown/json/html/sarif）
    - `--output` / `-o` 参数
    - `--verbose` / `-v` 参数
    - `--help` / `-h` 参数
  - **配置文件**：`miniapp-audit.config.json` 格式和字段说明
  - **扫描规则**：5 条规则的清单（名称、检测内容、严重等级）
  - **报告格式**：Markdown/JSON/HTML/SARIF 的说明和适用场景
  - **Web UI**：启动方式、页面说明
- 中文撰写，面向小程序开发者

## 依赖

- Issue 13-18（功能实现完成后写文档才准确）

## 验收

- 文档覆盖所有 CLI 参数和配置字段
- 快速开始命令可复制粘贴执行
- 配置文件示例可复制粘贴使用

## Triage

> *This was generated by AI during triage.*

- **Category**: `enhancement`
- **State**: `ready-for-agent`
- **Agent brief**: 编写完整的 `README.md`，覆盖安装、快速开始、CLI 参考、配置文件、规则清单、报告格式、Web UI 七个章节。中文撰写，面向小程序开发者。所有命令和配置示例可复制粘贴执行。等待 Issue 13-18 完成后再写（确保与实现一致）。
- **Blocked by**: Issue 13, Issue 14, Issue 15, Issue 16, Issue 17, Issue 18

## 完成状态

**Status: done**

### 完成内容

`README.md` 覆盖以下章节：
- **项目简介**：小程序上线前安全检查工具，本地扫描、不上传源码
- **当前能力**：5 条扫描规则表 + 4 种报告格式表 + 功能概述表
- **快速开始**：`npm install` → `npm test` → `npm run dev`
- **CLI 使用方式**：6 条示例覆盖 `--format` / `--output` / `--config` / `--verbose`
- **配置文件**：完整 JSON 示例 + 字段说明 + 禁用规则/排除目录两个场景示例
- **Web 使用方式**：4 个页面路由和功能表
- **验证命令**：`npm test` / `typecheck` / `lint` / `build` / `test:e2e` / `audit`
- **E2E Smoke Tests**：Playwright 安装和运行说明，覆盖路径说明
- **当前限制**：localhost only、localStorage、dependency-scanner 出网说明、非人工审计替代
- **技术栈**：TypeScript + Next.js + Vitest + Node.js >= 18

### 验证方式

README 中所有 CLI 示例均可直接复制粘贴执行。配置文件示例与 `src/scanner/config.ts` 的类型定义一致。

### 测试结果

`npm test`: 93 tests, 全部通过
`npm run typecheck`: 零错误
`npm run lint`: 零警告
`npm run build`: 成功
`npm run test:e2e`: 6/6 passed

### 剩余风险

- README 中 CLI 示例使用 `npx tsx src/run.ts`，正式发布前需改为 `npx miniapp-audit`
- 配置示例中的 adapter 名称需与代码中 `ScannerAdapter.name` 保持同步
