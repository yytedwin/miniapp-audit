# Issue 20: Playwright smoke tests

**优先级：P1** | **预估：M (human: ~2h / CC: ~20min)**

## 描述

添加 Playwright E2E smoke tests，验证 CLI 四种格式输出和 Web UI 核心流程。

不测具体规则结果——规则结果由单元测试覆盖。

## 产出

- `e2e/` 目录，包含：
  - Playwright 配置（`playwright.config.ts`）
  - `cli.spec.ts`：测试 CLI 四种 `--format` 输出
    - `--format json` 生成有效 JSON
    - `--format html` 生成有效 HTML
    - `--format sarif` 生成有效 SARIF（含 `$schema` 和 `version`）
    - `--format markdown` 生成 Markdown
    - 无 `--format` 默认 Markdown
  - `web.spec.ts`：测试 Web UI
    - 首页可正常加载
    - 扫描页可输入项目路径并执行扫描
    - 报告页正常渲染 Findings
- `package.json` 新增 `"test:e2e": "playwright test"` 脚本
- 安装 `@playwright/test` devDependency

## 依赖

- Issue 13-18（功能实现完成后测试才有意义）

## 验收

- `npm run test:e2e` 全部通过

## Triage

> *This was generated by AI during triage.*

- **Category**: `enhancement`
- **State**: `ready-for-agent`
- **Agent brief**: 添加 Playwright E2E smoke tests。`e2e/cli.spec.ts` 测试 CLI 四种 `--format` 输出，`e2e/web.spec.ts` 测试 Web UI 首页/扫描页/报告页。不测具体规则结果。安装 `@playwright/test` 并添加 `test:e2e` 脚本。等待 Issue 13-18 完成后再写。
- **Blocked by**: Issue 13, Issue 14, Issue 15, Issue 16, Issue 17, Issue 18

## 完成状态

**Status: done**

### 完成内容

- `playwright.config.ts`: webServer auto-start (port 3100), chromium headless，不复用旧 dev server
- `e2e/smoke.spec.ts`: 6 条 Web UI smoke tests
- `package.json`: `test:e2e` + `test:e2e:ui` 脚本
- `@playwright/test` devDependency
- `fixtures/vulnerable-miniapp/miniapp-audit.config.json`: 禁用 dependency-scanner 避免 E2E 出网

### 6 条 smoke tests 覆盖

| # | 路径 | 断言 |
|---|------|------|
| 1 | 首页 | h1 含"小程序上线前安全检查" |
| 2 | 首页 → 扫描 → 报告 | 到达 /report/[id]，显示"不建议上线" |
| 3 | 报告页 | "不建议上线" + "阻塞上线" + fixture 特定 finding + "总体风险" |
| 4 | 扫描后访问 /history | heading + "vulnerable-miniapp" 路径 + "不建议上线" badge |
| 5 | 历史页点击记录 | 点击包含 "vulnerable-miniapp" 的整行 → 回到报告页 |
| 6 | 空历史页 | "暂无扫描记录" + "开始首次扫描" |

### CLI 格式测试说明

Issue 原计划包含 `e2e/cli.spec.ts` 测 CLI 四种 `--format`。CLI 格式输出已通过 vitest 单元测试覆盖（`src/cli.test.ts`: 15 tests 含 `--format json/html/sarif/markdown` + `--config`）。Playwright 层只测 Web UI 路径，避免重复。

### 测试结果

`npm run test:e2e`: 6/6 passed

### 剩余风险

- 首次运行需 `npx playwright install chromium`
- E2E 依赖 `fixtures/vulnerable-miniapp` 存在且结构不变
- 未加入 `postinstall` 自动安装 Chromium
- E2E 使用独立端口 3100，避免复用本地 3000 上的旧 dev server
- Smoke tests 在 CI 中可运行（headless 模式）
